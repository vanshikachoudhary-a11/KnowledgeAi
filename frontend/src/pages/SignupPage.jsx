import { Link, useNavigate } from 'react-router-dom';
import { useState } from 'react';
import { useAuth } from '../hooks/useAuth';
import Button from '../components/Button';

export default function SignupPage() { const [name, setName] = useState(''); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const { authenticate } = useAuth(); const navigate = useNavigate(); const submit = (event) => { event.preventDefault(); authenticate({ name, email }); navigate('/dashboard'); }; return <div className="auth-page"><Link to="/" className="brand"><span className="brand-mark">✦</span> KnowledgeAI</Link><form className="auth-card" onSubmit={submit}><span className="eyebrow">GET STARTED</span><h1>Build a smarter knowledge base</h1><p>Create your account and turn documents into answers.</p><label>Name<input required value={name} onChange={(e) => setName(e.target.value)} placeholder="Your name" /></label><label>Email<input required type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="you@example.com" /></label><label>Password<input required minLength="8" type="password" value={password} onChange={(e) => setPassword(e.target.value)} placeholder="At least 8 characters" /></label><Button type="submit">Create account <span>→</span></Button><small>Already have an account? <Link to="/login">Log in</Link></small></form></div>; }
