import { Router } from 'express'; import { createChat, deleteChat, getChatMessages, listChats, renameChat, sendMessage, streamMessage } from '../controllers/chatController.js'; import { requireAuth } from '../middleware/authMiddleware.js';
const router = Router(); router.use(requireAuth); router.route('/').get(listChats).post(createChat); router.route('/:id').get(getChatMessages).patch(renameChat).delete(deleteChat); router.post('/:id/messages', sendMessage); router.post('/:id/messages/stream', streamMessage); export default router;
